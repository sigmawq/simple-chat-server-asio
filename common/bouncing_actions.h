//
// Created by swql on 12/12/20.
/*
 * Bouncing action is an action performed in response to a socket sending a message (bouncing_action_fptr)
 * EOF socket handler is invoked when trying to read but receiving EOF (eof_handler)
 * Both bouncing action and eof handler are defined in defs.h
 * ===
 * Common bouncing actions (for both client and server) are defined in this headers
 */
//

#ifndef SIMPLE_CHAT_SERVER_BOUNCING_ACTIONS_H
#define SIMPLE_CHAT_SERVER_BOUNCING_ACTIONS_H
#include "message.h"
#include "defs.h"

// Empty yet

#endif //SIMPLE_CHAT_SERVER_BOUNCING_ACTIONS_H
